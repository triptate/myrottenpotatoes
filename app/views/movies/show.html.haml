-# in app/views/movies/show.html.haml

%h2 Details about #{@movie.title}

%ul#details
  %li
    Rating:
    = @movie.rating
  %li
    Released on:
    = @movie.release_date.strftime("%B %d, %Y")

%h3 Description:

%p#description= @movie.description

= link_to 'Add Review', new_movie_review_path(@movie) 
|
= link_to ' Edit info', edit_movie_path(@movie) 
|
= link_to ' Delete', movie_path(@movie), :method => :delete 
|
- director_search = (@movie.director and @movie.director.length > 0) ? {:director => @movie.director} : {:flash_warning => "'#{@movie.title}' has no director info"}
= link_to ' Find movies with same director', movies_path(director_search)
|
= link_to ' Back to movie list', movies_path
